node_modules
dist
.expo
.expo-shared
*.log
.env
.env.local
.DS_Store
ios/Pods
android/.gradle
android/build
android/app/build
.turbo

# Local photo upload dir (only used when R2 is not configured)
apps/api/uploads

# Vercel build output (apps/api)
.vercel
